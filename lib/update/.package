(package update
  
  (title "Together Update")
  (description "Together Update")
  (authors "Guillaume Cartier")
  (root "src")
  (products (update (update update.settings)
                    (dependencies jazz)))
  (project ".project"))
